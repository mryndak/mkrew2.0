FROM liquibase/liquibase:4.30-alpine

# PostgreSQL JDBC driver is already included in liquibase:4.30-alpine
# in /liquibase/internal/lib/postgresql.jar

# Set working directory
WORKDIR /liquibase

# Default command will be overridden by docker-compose
CMD ["--help"]
